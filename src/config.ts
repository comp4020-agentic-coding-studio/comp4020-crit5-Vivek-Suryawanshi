/** Radius of the orbit the player moves on, in the same units as wave speed. */
export const ORBIT_RADIUS = 260;

/** Half the player's own angular width, in radians (3 degrees). */
export const PLAYER_ARC_HALF_WIDTH = (3 * Math.PI) / 180;
